#include<iostream>

using namespace std;

int main()
{
    // Equal to operator
    // int a = 10;
    // int b = 20;
    // int c = 20;
    // cout<<(a==b)<<endl;
    // cout<<(b==c)<<endl;

    // Not equal to operator

    int a = 10;
    int b = 20;
    int c = 20;
    cout<<(a!=b)<<endl; //  If the value is true(1) it Returns false(0).
    cout<<(b!=c)<<endl; //  If the value is false(0 )it Returns true(1).

    return 0;
}